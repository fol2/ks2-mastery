# Reading Stretch Challenge + Interface Hardening Package

Apply from the repository root after the Reading post-implementation hardening package is present.

Main patch:

```bash
git apply patches/001-reading-stretch-challenge-and-interface-hardening.patch
```

Then run the acceptance checks listed in `contract/reading-stretch-challenge-and-interface-hardening-contract.md`.

This package was prepared from a reconstructed local snapshot because the user-named `ks2-mastery-lean-05121226.zip` was not present in `/mnt/data`.
