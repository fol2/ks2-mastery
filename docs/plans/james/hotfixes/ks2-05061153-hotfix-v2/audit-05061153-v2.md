# KS2 Mastery 05061153 — Validation Resend Audit

This is a regenerated resend package for the Reading injected-clock scheduler bug found during validation of `ks2-mastery-lean-05061153.zip`.

Source boundary:

- Primary snapshot: uploaded `/mnt/data/ks2-mastery-lean-05061153.zip`.
- Patch source: regenerated from the ZIP’s `worker/src/subjects/reading/engine.js` and `tests/worker-reading-runtime.test.js`.
- Production state was not re-certified in this resend; this is a focused local hotfix delivery.

Bug:

Reading scheduling/read-model helpers still used wall-clock `Date.now()` in paths that should be deterministic under the injected Worker command clock.

Risk:

A future-dated skill or retry queue item can appear due during replay/tests/Hero-launched Reading sessions because the engine compares `dueAt` to the real current time instead of the injected command timestamp.

Patch effect:

- Threads `nowValue` through `nodeStatus()`, `questionWeakness()`, passage/question choice, `buildStats()`, and `buildAnalytics()`.
- Makes `analytics.generatedAt`, `stats.overview.due`, and `analytics.reviewQueue` respect the injected engine clock.
- Adds a regression test: `reading due stats and review queue use the injected command clock`.

Validated after applying the patch to a fresh extraction of the uploaded ZIP:

```text
node --check worker/src/subjects/reading/engine.js: PASS
node --check tests/worker-reading-runtime.test.js: PASS
node --test tests/worker-reading-runtime.test.js tests/reading-content-contract.test.js tests/reading-subject-registry.test.js: 16 pass / 0 fail
```

Touched files:

```text
worker/src/subjects/reading/engine.js
tests/worker-reading-runtime.test.js
```
